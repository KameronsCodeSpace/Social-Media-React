// log in user and define 
// in DB add if logged in or not / local storage / global variable true or false if logged in

const express = require('express');
const db = require('../pgPromise');

const Router = express.Router();

